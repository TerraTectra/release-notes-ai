name: 'AI Release Notes Generator'
description: 'Generate release notes by categorizing commit messages (feat, fix, other) without external APIs.'

outputs:
  release_notes:
    description: 'The generated release notes text.'

runs:
  using: 'node16'
  main: 'index.js'